using NUnit.Framework;

namespace MPT.Reporting.UnitTests
{
    [TestFixture]
    public class MessengerPromptListenerTests
    {
        // SubscribeListenerToMessageBox
        // UnsubscribeListenerToMessageBox
        // ReportMessageToMessageBox (w/ & w/o being handled) - Note: Seam needed to simulate MessageBox so that it does not appear & is testable
    }
}
